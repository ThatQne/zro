# deps
node_modules/
.pnpm-store/

# build output
dist/
dist-ssr/
src-tauri/target/
src-tauri/gen/schemas/

# tauri updater signing key — NEVER commit
*.key
zro.key
*.pem
signing-key*

# local-only: release tooling (publish GUI + secrets + staged artifacts) and
# the themed installer source stay off the public repo
tools/
zro-installer/

# env / secrets
.env
.env.*
!.env.example

# docs live on the website, not the repo — keep only these
*.md
!README.md
!CONTRIBUTING.md
!SECURITY.md
!LICENSE-COMMERCIAL.md

# local tooling / editor / os
.claude/
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.log

# misc
*.local
