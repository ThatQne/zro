# deps
node_modules/
.pnpm-store/

# build output
dist/
dist-ssr/
src-tauri/target/
src-tauri/gen/schemas/

# tauri updater signing key — NEVER commit
*.key
zro.key
*.pem
signing-key*

# env / secrets
.env
.env.*
!.env.example

# docs live on the website, not the repo — keep only the README
*.md
!README.md

# local tooling / editor / os
.claude/
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.log

# misc
*.local
